import React from 'react';
import { Typography, Box } from '@mui/material';

const AdminDashboard = () => {
    return (
        <Box>
            <Typography variant="h4">داشبورد مدیریت دیجی‌فیر</Typography>
            <Typography>به پنل مدیریت خوش آمدید.</Typography>
        </Box>
    );
};

export default AdminDashboard;